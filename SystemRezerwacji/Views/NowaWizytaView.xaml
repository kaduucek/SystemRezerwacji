<UserControl x:Class="SystemRezerwacji.Views.NowaWizytaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Lewa kolumna: wybór lekarza i daty -->
        <StackPanel Grid.Column="0" Margin="0,0,20,0">
            <TextBlock Text="Umów nową wizytę" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

            <TextBlock Text="Lekarz:"/>
            <ComboBox ItemsSource="{Binding DostepniLekarze}"
                      DisplayMemberPath="ImieINazwisko"
                      SelectedItem="{Binding WybranyLekarz}"
                      Margin="0,2,0,12"/>

            <TextBlock Text="Data:"/>
            <DatePicker SelectedDate="{Binding WybranaData}" Margin="0,2,0,12"/>

            <Button Content="Zarezerwuj wybrany termin"
                    Command="{Binding ZarezerwujCommand}"
                    Height="36" Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Prawa kolumna: wolne terminy -->
        <StackPanel Grid.Column="1">
            <TextBlock Text="Dostępne terminy:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
            <ListBox ItemsSource="{Binding WolneSloty}"
                     SelectedItem="{Binding WybranySlot}"
                     Height="350">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding StringFormat='dd.MM.yyyy   HH:mm'}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>

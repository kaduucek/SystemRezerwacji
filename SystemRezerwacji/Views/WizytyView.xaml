<UserControl x:Class="SystemRezerwacji.Views.WizytyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="Odśwież" Command="{Binding OdswiezCommand}"
                HorizontalAlignment="Left" Width="100" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" AutoGenerateColumns="False" ItemsSource="{Binding Wizyty}"
                  IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Data" Width="100"
                    Binding="{Binding DataGodzinaRozpoczecia, StringFormat='dd.MM.yyyy'}"/>
                <DataGridTextColumn Header="Godzina" Width="80"
                    Binding="{Binding DataGodzinaRozpoczecia, StringFormat='HH:mm'}"/>
                <DataGridTextColumn Header="Lekarz" Width="*"
                    Binding="{Binding Lekarz.ImieINazwisko}"/>
                <DataGridTextColumn Header="Status" Width="110"
                    Binding="{Binding Status}"/>
                <DataGridTemplateColumn Header="Akcje" Width="220">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Zmień termin" Margin="2" Padding="6,2"
                                    Command="{Binding DataContext.EdytujWizyteCommand,
                                              RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding IdWizyty}"/>
                                <Button Content="Anuluj" Margin="2" Padding="6,2"
                                    Command="{Binding DataContext.AnulujWizyteCommand,
                                              RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding IdWizyty}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

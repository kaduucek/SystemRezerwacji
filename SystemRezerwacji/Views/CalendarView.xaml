<UserControl x:Class="SystemRezerwacji.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Lekarz:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="DoctorBox" Width="260"
                      DisplayMemberPath="ImieINazwisko"
                      SelectionChanged="DoctorBox_SelectionChanged"/>
        </StackPanel>

        <Calendar Grid.Row="1" Grid.Column="0" x:Name="AppointmentCalendar"
                  SelectedDatesChanged="AppointmentCalendar_SelectedDatesChanged"/>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="20,0,0,0">
            <TextBlock Text="Dostępne terminy:" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
            <ListBox x:Name="SlotsList" Height="400">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding StringFormat='dd.MM.yyyy   HH:mm'}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>

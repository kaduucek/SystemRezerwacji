<UserControl x:Class="SystemRezerwacji.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             d:DesignHeight="450" d:DesignWidth="500"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="320">
            <TextBlock Text="Rejestracja" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Imię"/>
            <TextBox Text="{Binding Imie, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,10"/>

            <TextBlock Text="Nazwisko"/>
            <TextBox Text="{Binding Nazwisko, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,10"/>

            <TextBlock Text="E-mail"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,10"/>

            <TextBlock Text="Hasło (min. 6 znaków)"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,2,0,20"/>

            <Button Content="Zarejestruj się"
                    Command="{Binding ZarejestrujCommand}"
                    CommandParameter="{Binding ElementName=PasswordBox, Path=Password}"
                    Height="32"/>

            <Button Content="Powrót do logowania"
                    Command="{Binding PowrotCommand}"
                    Margin="0,10,0,0" Background="Transparent" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</UserControl>

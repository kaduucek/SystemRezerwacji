<Window x:Class="SystemRezerwacji.Views.EdytujWizyteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zmiana terminu wizyty" Height="260" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Lekarz:" Margin="5" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="0" Grid.Column="1" Margin="5"
                  ItemsSource="{Binding DostepniLekarze}"
                  DisplayMemberPath="ImieINazwisko"
                  SelectedItem="{Binding WybranyLekarz}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Data:" Margin="5" VerticalAlignment="Center"/>
        <DatePicker Grid.Row="1" Grid.Column="1" Margin="5"
                    SelectedDate="{Binding NowaData}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Godzina:" Margin="5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="5"
                 Text="{Binding NowaGodzina, UpdateSourceTrigger=PropertyChanged}"
                 ToolTip="Format GG:MM, np. 09:30"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="5">
            <Button Content="Zapisz" Command="{Binding ZapiszCommand}" Width="90" Margin="5"/>
            <Button Content="Anuluj" Command="{Binding AnulujCommand}" Width="90" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>

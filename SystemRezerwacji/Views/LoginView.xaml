<UserControl x:Class="SystemRezerwacji.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SystemRezerwacji.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="350">
            <TextBlock Text="Logowanie" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            <TextBox Text="{Binding Email}" PlaceholderText="E-mail" Margin="0,0,0,10"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,20"/>
            <Button Content="Zaloguj się" Command="{Binding LoginCommand}" Width="200"/>
            <Button Content="Nie masz konta? Zarejestruj się"
            Command="{Binding PrzejdzDoRejestracji}" Margin="0,10,0,0"/>
        </StackPanel>

    </Grid>
</UserControl>

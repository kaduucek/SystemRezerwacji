<UserControl x:Class="SystemRezerwacji.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             d:DesignHeight="450" d:DesignWidth="500"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="320">
            <TextBlock Text="Logowanie" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="E-mail"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,10"/>

            <TextBlock Text="Hasło"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,2,0,20"/>

            <Button Content="Zaloguj się"
                    Command="{Binding LoginCommand}"
                    CommandParameter="{Binding ElementName=PasswordBox, Path=Password}"
                    Height="32"/>

            <Button Content="Nie masz konta? Zarejestruj się"
                    Command="{Binding PrzejdzDoRejestracjiCommand}"
                    Margin="0,10,0,0" Background="Transparent" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</UserControl>
